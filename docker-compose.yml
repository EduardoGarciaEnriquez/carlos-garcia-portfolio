services:
  frontend:
    image: frontend
    container_name: portfolio-frontend-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=development
      - PORT=3000
    env_file:
      - .env
